name: growmate_reminder
description: Crop Reminder System for Farmers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^16.3.0  # ADD THIS
  timezone: ^0.9.0                      # ADD THIS for timezone support
  intl: ^0.18.0                         # ADD THIS for date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true 